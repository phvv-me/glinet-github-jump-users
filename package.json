{
  "name": "glinet-github-jump-users",
  "version": "0.0.2",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "view": "node scripts/build-view.mjs",
    "test": "pnpm run view && sh scripts/test.sh",
    "build": "pnpm run test && sh scripts/build.sh",
    "feed": "pnpm run build && node scripts/build-feed.mjs"
  },
  "devDependencies": {
    "vue-template-compiler": "2.7.16",
    "vue-template-es2015-compiler": "1.9.1"
  }
}
